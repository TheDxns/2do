spring:
  h2.console:
    enabled: true
    path: /console
    settings.web-allow-others: true
  datasource.url: jdbc:h2:file:/C:/Workspace/todo/testdb
  
management:
  endpoints:
    web:
      exposure:
        include: '*'